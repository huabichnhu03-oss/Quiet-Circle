# ──────────────────────────────────────────────────────────────────────────────
# MoodMind — Environment Variables
# Copy this file to .env and fill in your own values before running locally.
# NEVER commit your real .env file (it is already in .gitignore).
# ──────────────────────────────────────────────────────────────────────────────

# Clerk — authentication (https://dashboard.clerk.com)
# Vite only exposes VITE_* (and NEXT_PUBLIC_*) vars to the browser at build time.
# Local dev: use pk_test_ / sk_test_ (pk_live_ only works on quiet-circle.app).
# Vercel Production: use pk_live_ / sk_live_ — set in Vercel dashboard, NOT in committed files.
# Optional: keep live key backups locally as *_LIVE (app does not read them). See CAUTION.md.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Where to send users after OAuth / sign-in (optional; defaults shown)
VITE_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
VITE_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/onboarding

# Optional: disable Clerk "Client Trust" (new-device email code) for ALL users in dev:
#   npm run bypass-client-trust
# Or turn it off globally in Clerk Dashboard → Configure → Attack protection → Client Trust.

# PostgreSQL connection string (recommended: Neon free tier — https://neon.tech)
# Without this, wellness data stays in memory and is lost on restart.
DATABASE_URL=postgresql://postgres:password@localhost:5432/moodmind

# Secret used to sign express-session cookies (future use).
SESSION_SECRET=replace-me-with-a-long-random-string

# Public app URL (production: https://www.quiet-circle.app)
APP_URL=http://localhost:5000

# (Optional) Port for the local dev server. Defaults to 5000 if not set.
# PORT=5000
